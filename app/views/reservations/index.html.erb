

<nav>
    <div class="nav-wrapper grey darken-1" style="padding-left: 1%; padding-right: 60%">
        <form>
        <div class="input-field">
            <input id="search" type="search" required>
            <label class="label-icon" for="search"><i class="material-icons">search</i></label>
            <i class="material-icons">close</i>
        </div>
        </form>
    </div>
    <div class="collection" style="padding-left: 1%; padding-right: 1%">
        <% @reservations.each do |res| %>
        <a href="#!" class="collection-item" style="border: 1px solid #cfd8dc"><%= res.trip_start %> - <%= res.customer_id %></a>
        <% end %>
    </div>
</nav>